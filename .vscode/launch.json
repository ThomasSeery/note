{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug note",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/gdb/bin/note",
            "args": ["add", "milk"],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "miDebuggerPath": "/usr/bin/gdb"
        }
    ]
}
